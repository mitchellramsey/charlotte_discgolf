

<!-- Navbar -->
  <div class="collapse navbar-collapse" id="navbarText">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <a class="nav-link courses-login" href="/homepage">Home <span class="sr-only">(current)</span></a>
      </li>
      <li class="nav-item">
        <a class="nav-link courses-login" href="/courses">Courses</a>
      </li>
    </ul>
    <span class="navbar-text">
        {{#if personList}}
            <p class="text-right courses-login">Welcome, <a href="/userprofile"><span class="navbar-text">
              {{#each personList}}
                {{req.user.username}}
              {{/each}}
              <button href="/auth/logout/" class="btn btn-primary">Log Out</button>
            </span></a></p>
        {{/if}} 
    </span>
  </div>