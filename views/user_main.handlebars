<body>
   <div class="container">
      <h1 class="display-4 text-center course-header">Profile</h1>
   </div>
   <div class="container">
      <div class="row">
         <div class="col-md-12 text-center">
            <table class="table table-striped table-dark">
               <thead>
                  <tr>
                     <th scope="col">Name</th>
                     <th scope="col">Recent Rounds</th>
                     <th scope="col"><button type="button" class="btn btn-primary">Play New Round</button></th>
                  </tr>
               </thead>
               <tbody>
                  <tr>
                     <td>
                        {{#if usersList.user_name}}
                        {{usersList.user_name}}
                        {{/if}} 
                     </td>
                  </tr>
               </tbody>
            </table>
         </div>
      </div>
   </div>
</body>